/** Max number of cards in a project. */
export const MAX_CARDS = 16;

/** Final exported card side in pixels (PNG/PDF). */
export const CARD_EXPORT_SIZE = 2050; 

/** Lightweight exact-preview side in pixels (downscaled from export). */
export const PREVIEW_EXACT_SIZE = 768;

/** Fast preview side for in-grid/quick previews. */
export const PREVIEW_FAST_SIZE = 1024;

/** Default JPEG quality for PDF export (0..1). */
export const PDF_JPEG_QUALITY = 0.80;